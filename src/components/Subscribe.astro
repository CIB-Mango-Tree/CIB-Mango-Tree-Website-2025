---
import Button from "./Button.astro";

const subscribeLink = (event) => {
  event.preventDefault();
  const email = event.target.email.value;
  console.log('Email submitted:', email);
  // TODO: Add logic to handle the email
};
---
<div class="bg-[#FFD566] flex flex-wrap items-center px-3 sm:px-[20svw] py-8"> 
    <h3 class="text-2xl w-full pb-4">Subscribe to our Newsletter</h3>
    <form onsubmit={subscribeLink} class="flex w-full">
    <input
        type="email"
        name="email"
        placeholder="Enter your email"
        required
        class="bg-white px-4 py-2 border border-gray-300 size-fit rounded-md flex-1 mr-4"
    />
    <Button styles="bg-[#609949] text-white" onClick={subscribeLink}>Subscribe</Button>
    </form>
<div>
<style>

</style>